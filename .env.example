# Platform (admin signin)
BASE_URL=http://127.0.0.1:3000

# Admin (Platform Owner)
ADMIN_EMAIL=zudogu.official@gmail.com
ADMIN_PASSWORD=ZudoguAdmin2026!

# Tenant (Store Owner) — signin on tenant host
TENANT_BASE_URL=http://yourstore.localhost:3000
TENANT_EMAIL=luesat.d@outlook.com
TENANT_PASSWORD=D12345!

# Shopper (Customer) — same host as storefront
SHOPPER_EMAIL=Luesat.d@gmail.com
SHOPPER_PASSWORD=D12345!

# Seed / E2E (optional overrides)
E2E_PRODUCT_1_SLUG=qa-e2e-premium-dog-food-15kg
E2E_PRODUCT_2_SLUG=qa-e2e-cat-teaser-wand
E2E_COUPON_CODE=QA_E2E_DISCOUNT50
E2E_PLATFORM_SERVICE_FEE=35
E2E_PLATFORM_SERVICE_COST=20
E2E_BASE_SHIPPING_COST=50
E2E_CUSTOM_SHIPPING_RATE=45

# Optional: run auth before every `npx playwright test` (slow)
# RUN_AUTH_BEFORE_TESTS=1

# Skip auth in global-setup / skip generating state in CI
# SKIP_GLOBAL_AUTH=1

# Optional: show browser during auth:setup
# HEADED=1
